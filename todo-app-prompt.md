# TO-DO 캘린더 웹앱 만들기

## 진행 방식 (가장 중요)
1. 코드를 바로 작성하지 말고, 먼저 **폴더 구조 + 구현 계획 + 사용할 라이브러리와 이유**를 보여줘.
2. 내가 승인하면 아래 "구현 순서"대로 **한 단계씩만** 진행해줘.
3. 각 단계가 끝나면 멈추고 다음을 알려줘:
   - 실행 방법 (터미널 명령어)
   - 브라우저에서 직접 확인할 체크리스트
   - 이번 단계에서 바뀐 파일 목록
4. 단계가 끝날 때마다 git 커밋해줘 (메시지 예: `step 3: 캘린더 월간 뷰`).
5. 계획에 없는 라이브러리를 추가하려면 먼저 이유를 설명하고 내 동의를 받아줘.
6. 내 요구사항이 모호하거나 서로 충돌하면 추측하지 말고 질문해줘.
7. 외부 서비스 가입, 키 발급, 환경변수 설정처럼 내가 직접 해야 하는 일은 클릭 순서까지 단계별로 안내해줘.

---

## 앱 목적
일정과 루틴을 관리하는 앱이다.
캘린더가 화면 중심에 있고, 날짜를 누르면 캘린더 아래에 그날의 할 일이 나온다.
월간과 일간을 한 화면에서 함께 관리하는 것이 목표다.

---

## 기술 스택

### 프론트엔드
- **React + Vite + TypeScript**: 빠른 개발 서버. 타입이 있어서 데이터 구조 실수를 줄여 준다.
- **Tailwind CSS**: 둥근 모서리, 색상 같은 디자인 규칙을 일관되게 적용하기 쉽다.
- **Zustand (+ persist 미들웨어)**: 전역 상태 관리. localStorage 자동 저장을 간단히 처리한다.
- **@dnd-kit**: 할 일을 드래그해서 다른 카테고리로 옮기는 기능. 모바일 터치를 지원한다.
- **date-fns**: 날짜 계산 (월 이동, 요일, 내일/오늘 판단).
- **vite-plugin-pwa**: 서비스 워커 + manifest. 푸시 알림 수신과 홈 화면 추가에 필요하다 (아이폰은 홈 화면에 추가해야만 푸시를 받을 수 있음).
- 폰트: Pretendard (한글 가독성)

### 데이터 저장
- 할 일, 카테고리, 메모, 루틴은 모두 **브라우저 localStorage**에 저장한다 (로그인 없음).
- 로그인 대신 첫 실행 시 기기 ID(uuid)를 만들어 localStorage에 저장한다.

### 알림 (탭이 닫혀 있어도 알림이 와야 함)
알림만 가벼운 서버 기능을 사용한다.
- **Vercel Functions** (`/api` 폴더): 구독 등록, 알림 동기화, 알림 발송 API
- **Upstash Redis**: 기기별 푸시 구독 정보와 알림 목록 저장 (할 일 제목, 알림 시간, 루틴 규칙, 완료 여부만. 할 일 전체 데이터는 올리지 않음)
- **Upstash QStash**: 5분마다 `/api/send-due` 호출 (Vercel Hobby 플랜 크론은 하루 1회 제한이라 사용하지 않음)
- **web-push**: VAPID 키로 푸시 발송
- 모든 비밀값(VAPID 키, Upstash 토큰, QStash 서명 키)은 Vercel 환경변수로 관리하고 코드에 넣지 않는다.

### 배포
- Vercel

---

## 추천 폴더 구조 (더 나은 구조가 있으면 제안해줘)
```
api/                         # Vercel Functions (알림 서버)
├─ subscribe.ts              # 푸시 구독 등록
├─ sync-alarms.ts            # 기기의 알림 목록 동기화
├─ send-due.ts               # QStash가 5분마다 호출 → 울릴 알림 발송
└─ _lib/
   ├─ redis.ts
   ├─ push.ts                # web-push 설정
   └─ due.ts                 # 지금 울려야 할 알림 계산 (Asia/Seoul 기준, 루틴 포함)
public/
├─ icons/                    # PWA 아이콘
src/
├─ main.tsx
├─ App.tsx
├─ sw.ts                     # 서비스 워커 (푸시 수신, 알림 클릭 처리)
├─ data/
│  └─ defaults.json          # 기본 카테고리, 색상 팔레트, 문구 (코드 수정 없이 변경 가능)
├─ types/
│  └─ index.ts               # Category, Task, Routine, DayMemo, AlarmPayload 타입
├─ store/
│  └─ useAppStore.ts         # Zustand 스토어 + localStorage 저장
├─ lib/
│  ├─ date.ts                # 날짜 유틸 (YYYY-MM-DD 변환 등)
│  ├─ routine.ts             # 루틴 규칙 → 날짜별 할 일 계산 (서버와 공유 가능하게)
│  ├─ stats.ts               # 날짜별 완료 통계 (캘린더 색상용)
│  ├─ push.ts                # 알림 권한 요청, 푸시 구독
│  └─ alarmSync.ts           # 알림 변경 시 서버 동기화 (오프라인 대기열 포함)
├─ components/
│  ├─ layout/BottomBar.tsx
│  ├─ memo/DayMemo.tsx
│  ├─ calendar/Calendar.tsx, DayCell.tsx, DayRing.tsx
│  ├─ category/CategoryMenu.tsx, CategoryGroup.tsx
│  ├─ task/TaskItem.tsx, TaskDetailModal.tsx
│  └─ pickers/DatePickerModal.tsx, AlarmPickerModal.tsx, RoutineModal.tsx
└─ styles/index.css
```

---

## 데이터 구조
날짜는 모두 `"YYYY-MM-DD"` 문자열로 저장한다 (시간대 버그 방지). 시간 계산의 기준 시간대는 **Asia/Seoul**이다.
localStorage에는 키 하나(`todo-app:v1`)로 저장하고, 나중에 구조가 바뀌면 마이그레이션할 수 있게 `version` 필드를 둔다.

```ts
type Category = {
  id: string;
  name: string;
  color: string;        // defaults.json 팔레트 중 하나
  order: number;
  collapsed: boolean;   // 트리 접기/펼치기
};

type Alarm = { hour: number; minute: number };  // hour 0~23, minute 0~55 (5분 단위)

type Task = {
  id: string;
  title: string;
  categoryId: string;
  date: string;         // "2026-09-22"
  done: boolean;
  doneAt?: string;
  memo: string;
  alarm: Alarm | null;
  routineId?: string;   // 루틴에서 생성된 할 일이면 연결
  order: number;
};

type Routine = {
  id: string;
  title: string;
  categoryId: string;
  rule:
    | { type: "daily" }
    | { type: "weekly"; daysOfWeek: number[] }   // 0=일 ~ 6=토
    | { type: "monthly"; dayOfMonth: number };
  startDate: string;
  endDate?: string;
  alarm: Alarm | null;
  skippedDates: string[];   // 특정 날짜만 삭제/이동한 경우
};

type AppData = {
  version: 1;
  deviceId: string;
  categories: Category[];
  tasks: Task[];
  routines: Routine[];
  dayMemos: Record<string, string>;   // { "2026-09-22": "메모 내용" }
};
```

**루틴 처리 방식**: 루틴은 규칙만 저장한다. 화면에는 날짜별로 "가상 할 일"로 계산해서 보여준다.
사용자가 체크, 편집, 이동하는 순간에만 실제 Task로 저장한다.
이렇게 하면 미래 날짜 데이터가 무한히 쌓이지 않는다.

**서버로 보내는 알림 데이터 (Redis)**:
- `sub:{deviceId}` → 푸시 구독 정보
- `alarms:{deviceId}` → 알림 목록
  - 일반 할 일: `{ taskId, title, date, hour, minute, done }`
  - 루틴: `{ routineId, title, rule, startDate, endDate, skippedDates, doneDates, hour, minute }`
- `sent:{deviceId}:{alarmKey}:{date}` → 발송 기록 (중복 방지, 며칠 뒤 자동 만료)

---

## 화면 구성 (단일 화면, 위에서 아래 순서)

### 1. 상단 헤더
- 왼쪽: 현재 보고 있는 연·월 (예: 2026년 9월), 좌우 화살표로 월 이동
- 오른쪽: 세 줄(☰) 버튼 → 카테고리 메뉴

### 2. 날짜별 메모
- 캘린더 위에 메모 입력칸이 있다.
- 전체 메모가 아니라 **선택한 날짜의 메모**다. 저장을 누르면 그 날짜에만 저장된다.
- 날짜를 바꾸면 해당 날짜의 메모가 표시된다.
- 메모가 있는 날짜는 캘린더 칸에 작은 점을 표시한다.

### 3. 캘린더 (월간)
- 지난 날짜도 누를 수 있다. 지난 날짜에서도 할 일 추가, 카테고리 이동이 가능하다.
- **오늘 날짜를 눈에 띄게 표시한다** (굵은 테두리 링 + 강조색 숫자).
- 선택한 날짜는 오늘 표시와 다른 방식으로 표시한다 (채워진 배경).
- **각 날짜 칸에는 동그란 원이 있다**:
  - 할 일이 없으면: 회색 원 안에 `0`
  - 할 일이 있으면: 원 안의 숫자가 **그날 전체 할 일 개수**로 바뀐다 (카테고리 구분 없이 합계)
  - 완료한 할 일이 생기면 원이 카테고리 색으로 채워진다
    - 색은 섞지 않고 파이/도넛처럼 구역을 나눈다 (CSS `conic-gradient` 권장)
    - 각 구역의 크기는 카테고리별 완료 개수에 비례한다. 가장 많이 완료한 카테고리 색이 가장 큰 구역을 차지한다
    - 한 카테고리만 완료했다면 그 색이 원 전체를 차지한다
    - 완료 전인 날은 회색 원이다
- 캘린더를 좌우로 스와이프해서 월을 이동할 수 있다 (모바일).

### 4. 할 일 목록 (선택한 날짜)
- 캘린더 아래에 선택한 날짜가 표시된다 (예: 9월 22일 (화)).
- 카테고리별로 묶여 있고, 할 일은 카테고리 아래로 트리처럼 들여쓰기되어 내려간다.
- 카테고리 이름 옆의 `+` 버튼으로 그 카테고리에 할 일을 추가한다 (인라인 입력, Enter로 저장).
- 할 일 왼쪽의 동그란 체크박스로 완료/미완료를 토글한다.
  - 완료 시 살짝 튀는 애니메이션, 취소선, 흐리게 처리
- 할 일을 **드래그해서 다른 카테고리로 옮길 수 있다**. 같은 카테고리 안에서 순서도 바꿀 수 있다.
  - 모바일에서는 길게 누르면 드래그 시작 (스크롤과 충돌 방지)
- 루틴에서 생긴 할 일에는 작은 반복(↻) 아이콘, 알림이 있는 할 일에는 시간을 표시한다.
- 할 일이 없는 날에는 빈 상태 안내 문구를 보여준다 (예: "카테고리 옆 +를 눌러 할 일을 추가해보세요").

### 5. 하단 바
- 맨 아래에 **집 모양 아이콘 하나만** 있다.
- 누르면 오늘 날짜로 이동하고 오늘이 선택된다.

---

## 카테고리 메뉴 (☰)
- 오른쪽에서 슬라이드되어 나오는 패널
- 카테고리 목록 + 하단의 **"Add a list"** 버튼
- 추가할 때 이름과 색을 고른다 (defaults.json 팔레트에서 선택)
- 카테고리 이름 변경, 색 변경, 삭제
  - 삭제 시 확인창을 띄운다. 안에 할 일이 있으면 경고한다
- 카테고리 순서 드래그 변경
- 패널 하단: 데이터 "내보내기(JSON 다운로드)" / "가져오기" 버튼
  (localStorage는 브라우저 데이터를 지우면 사라지기 때문에 필요)

---

## 할 일 상세 창
- 할 일을 누르면 **화면 중앙에 화면 높이의 약 2/3를 차지하는 창**이 뜬다. 배경은 어둡게 처리하고, 바깥을 누르면 닫힌다.
- 포함 항목:
  1. **할 일 이름** (상단 표시)
  2. **편집**: 이름 수정
  3. **삭제**: 삭제 후 하단에 "실행 취소" 토스트를 몇 초간 표시
  4. **메모**: 할 일별 메모
  5. **알림 설정**
  6. **내일 하기**: 날짜를 다음 날로 이동
     - 지난 날짜의 할 일이면 이 버튼은 **"오늘 하기"**로 바뀌고, 누르면 오늘로 이동한다
  7. **날짜 바꾸기**: 캘린더 창이 뜨고, 날짜를 누른 뒤 **Confirm**을 눌러야 적용된다
  8. **루틴 설정하기**

### 알림 설정 창
- **Hour**: 0~23, **Minute**: 0~55 (5분 단위)
- 스크롤 휠 또는 선택 목록 형태로, Hour와 Minute을 각각 선택한다
- **Done**을 눌러야 설정된다
- Done 왼쪽에 **Delete**가 있고, 누르면 알림이 삭제된다
- 안내 문구:
  - 권한이 거부된 경우: 브라우저 설정에서 허용하는 방법을 안내
  - 아이폰 Safari에서 홈 화면에 추가하지 않은 경우: "홈 화면에 추가해야 알림을 받을 수 있어요"
  - 알림은 설정 시각 기준 최대 몇 분 늦게 올 수 있다는 작은 안내

### 루틴 설정 창
- 반복 방식: 매일 / 매주 (요일 다중 선택) / 매월 (날짜 선택)
- 시작일은 현재 할 일의 날짜로 한다. 종료일은 선택 사항이다
- 루틴 할 일을 삭제할 때는 선택지를 준다: "이 날짜만 삭제" / "이후 전부 삭제"

---

## 알림 동작 방식
1. 사용자가 처음 알림을 설정하면 알림 권한을 요청하고, 푸시 구독을 만들어 `/api/subscribe`로 보낸다
2. 알림을 추가/수정/삭제하거나, 할 일의 날짜·완료 상태·루틴이 바뀌면 그 기기의 알림 목록을 `/api/sync-alarms`로 동기화한다
   - 루틴 할 일은 개별 날짜가 아니라 루틴 규칙 자체를 보낸다
   - 완료한 할 일은 알림을 보내지 않는다
   - 오프라인 상태에서 바뀐 내용은 대기열에 두었다가 다시 연결되면 동기화한다
3. QStash가 5분마다 `/api/send-due`를 호출한다. 이 API는 **QStash 서명을 검증**한 뒤,
   Asia/Seoul 기준으로 지금 울려야 할 알림을 찾아 푸시를 보낸다
4. 서비스 워커가 푸시를 받아 알림을 표시하고, 알림을 누르면 앱이 열리면서 해당 날짜로 이동한다
5. 같은 알림이 두 번 가지 않도록 발송 기록을 남긴다
6. 만료된 구독(410 응답)은 자동으로 삭제한다
7. API는 deviceId와 입력 형식을 검증하고, 너무 큰 요청은 거부한다

---

## 디자인 규칙
- **모든 요소는 각이 없고 둥근 테두리**를 가진다 (버튼, 카드, 입력창, 모달, 체크박스 전부)
- 모바일 우선 설계. 데스크톱에서는 가운데 정렬, 최대 너비 약 480px
- 부드러운 파스텔 톤 카테고리 팔레트 (8~10색). 회색은 "미완료"용이라 팔레트에서 제외
- 그림자는 은은하게, 여백은 넉넉하게
- 전환 애니메이션은 짧고 부드럽게 (모달 열림, 체크, 패널 슬라이드)
- 다크 모드: 시스템 설정을 따른다
- 터치 영역은 최소 44px
- 첫 실행 시 defaults.json의 기본 카테고리 2~3개를 생성한다 (예: 개인, 공부, 운동)

---

## 구현 순서
1. **프로젝트 세팅**: Vite + React + TS + Tailwind, 폰트, 기본 레이아웃, 하단 집 아이콘 바
2. **데이터 계층**: 타입 정의, defaults.json, Zustand 스토어 + localStorage 저장, 기기 ID, 첫 실행 초기화
3. **캘린더**: 월간 뷰, 월 이동, 오늘 강조, 날짜 선택, 지난 날짜 선택, 집 아이콘으로 오늘 이동
4. **카테고리**: ☰ 메뉴, Add a list, 색 선택, 이름/색 변경, 삭제
5. **할 일 기본**: + 버튼으로 추가, 체크 토글, 트리 표시, 접기/펼치기
6. **캘린더 원 표시**: 전체 할 일 개수 숫자, 완료 카테고리 색 비율 표시 (conic-gradient)
7. **날짜별 메모**: 캘린더 위 메모 입력/저장, 메모 있는 날짜 점 표시
8. **할 일 상세 창**: 편집, 삭제(+실행 취소), 메모, 내일 하기/오늘 하기, 날짜 바꾸기(Confirm)
9. **루틴**: 반복 규칙, 가상 할 일 계산, 이 날짜만/이후 전부 삭제
10. **드래그 앤 드롭**: 카테고리 간 이동, 순서 변경, 모바일 길게 누르기
11. **첫 Vercel 배포**: 빌드 확인, GitHub 연결, 배포 방법 안내 (푸시 알림은 배포된 HTTPS 환경에서 테스트해야 하므로 알림 전에 배포)
12. **알림**
    - 12-1. PWA 설정: 서비스 워커, manifest, 앱 아이콘, 홈 화면 추가 확인
    - 12-2. 알림 UI: Hour/Minute 선택, Done/Delete, 안내 문구 (아직 로컬에만 저장)
    - 12-3. 서버 준비: Upstash 가입(Redis, QStash), VAPID 키 생성, Vercel 환경변수 설정을 단계별로 안내
    - 12-4. 구독 + 동기화 API: `/api/subscribe`, `/api/sync-alarms`, 오프라인 대기열
    - 12-5. 발송 API + 스케줄: `/api/send-due`, QStash 5분 스케줄 등록, 개발 모드 전용 "지금 알림 보내기" 테스트 버튼
    - 12-6. 알림 테스트 체크리스트: 탭 닫은 상태, 휴대폰 화면 꺼진 상태, 루틴 알림, 완료한 할 일 알림 안 오는지
13. **마무리**: 백업/복원, 다크 모드, 애니메이션, 빈 상태, 모바일 점검, 최종 배포

지금은 1번을 시작하지 말고, 폴더 구조와 구현 계획부터 보여줘.
