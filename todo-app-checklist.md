# TO-DO 캘린더 앱 — 단계별 체크리스트

각 단계가 끝나면 아래 항목을 직접 확인하고 체크하세요.
하나라도 안 되면 다음 단계로 넘어가지 말고 Claude Code에게 "○번 항목이 이렇게 안 돼"라고 알려주세요.

**모든 단계 공통**
- [ ] `npm run dev` 실행 시 에러 없이 열린다
- [ ] 브라우저 개발자 도구(F12) → Console 탭에 빨간 에러가 없다
- [ ] 새로고침해도 데이터가 그대로 남아 있다 (2단계 이후)
- [ ] 휴대폰 크기로 봐도 깨지지 않는다 (개발자 도구 → 기기 아이콘 → iPhone 등 선택)
- [ ] git 커밋이 되었다 (`git log`로 확인)

---

## 1. 프로젝트 세팅
- [ ] 브라우저에서 앱 화면이 뜬다
- [ ] 하단에 집 아이콘 하나만 있다
- [ ] Pretendard 폰트가 적용되었다 (한글 모양이 기본 폰트와 다름)
- [ ] 화면이 가운데 정렬되고, 컴퓨터에서도 너비가 휴대폰 크기(약 480px)로 유지된다
- [ ] 버튼·카드 등 기본 요소에 각진 모서리가 없다

## 2. 데이터 계층
- [ ] 처음 열면 기본 카테고리 2~3개가 생긴다
- [ ] 개발자 도구 → Application → Local Storage에 `todo-app:v1` 키가 있다
- [ ] 그 안에 `version`, `deviceId`, `categories`, `tasks`, `routines`, `dayMemos`가 있다
- [ ] 새로고침해도 기본 카테고리가 중복으로 생기지 않는다
- [ ] Local Storage를 지우고 새로고침하면 다시 기본 카테고리가 생긴다
- [ ] `defaults.json`에서 카테고리 이름을 바꾸고 Local Storage를 지우면, 바뀐 이름으로 생긴다

## 3. 캘린더
- [ ] 이번 달(현재 날짜 기준)이 표시된다
- [ ] 요일이 올바르게 맞는다 (예: 2026년 9월 22일이 화요일)
- [ ] 오늘 날짜가 확실히 눈에 띈다
- [ ] 날짜를 누르면 선택 표시가 되고, 오늘 표시와 구분된다
- [ ] 좌우 화살표로 이전/다음 달 이동이 된다
- [ ] 12월 → 1월, 1월 → 12월로 넘어갈 때 연도가 바뀐다
- [ ] 2월(28/29일), 31일인 달이 올바르게 나온다
- [ ] 지난 날짜도 누를 수 있다
- [ ] 다른 달로 이동한 뒤 집 아이콘을 누르면 오늘로 돌아오고 오늘이 선택된다
- [ ] 휴대폰 크기에서 좌우 스와이프로 월 이동이 된다

## 4. 카테고리
- [ ] ☰ 버튼을 누르면 오른쪽에서 패널이 나온다
- [ ] 패널 바깥을 누르면 닫힌다
- [ ] Add a list로 이름과 색을 골라 카테고리를 추가할 수 있다
- [ ] 이름을 비워둔 채 추가하면 막히거나 안내가 나온다
- [ ] 카테고리 이름·색을 바꿀 수 있다
- [ ] 카테고리를 삭제하면 확인창이 뜬다
- [ ] 할 일이 들어 있는 카테고리를 삭제할 때 경고가 나온다
- [ ] 카테고리 순서를 드래그로 바꿀 수 있다
- [ ] 새로고침해도 모든 변경이 유지된다

## 5. 할 일 기본
- [ ] 카테고리 옆 + 버튼으로 할 일을 추가할 수 있다 (Enter로 저장)
- [ ] 빈 칸으로는 추가되지 않는다
- [ ] 할 일이 카테고리 아래에 들여쓰기되어 나온다
- [ ] 동그란 체크박스로 완료/미완료가 바뀐다 (취소선, 흐리게, 애니메이션)
- [ ] 다른 날짜를 누르면 그 날짜의 할 일만 나온다
- [ ] 지난 날짜에도 할 일을 추가할 수 있다
- [ ] 카테고리를 접으면 할 일이 숨겨지고, 펼치면 다시 나온다
- [ ] 할 일이 없는 날에는 안내 문구가 나온다
- [ ] 아주 긴 제목을 넣어도 화면이 깨지지 않는다

## 6. 캘린더 원 표시
- [ ] 할 일이 없는 날짜는 회색 원에 `0`
- [ ] 할 일을 3개 추가하면 그 날짜의 숫자가 `3`이 된다 (카테고리 상관없이 합계)
- [ ] 할 일을 지우면 숫자가 줄어든다
- [ ] 한 카테고리의 할 일만 완료하면 그 색이 원 전체를 채운다
- [ ] 두 카테고리를 완료하면 색이 섞이지 않고 구역으로 나뉜다
- [ ] 완료 개수가 많은 카테고리 색이 더 큰 구역을 차지한다 (예: A 3개, B 1개 → A가 3/4)
- [ ] 완료를 취소하면 색이 다시 줄어들거나 회색이 된다
- [ ] 카테고리 색을 바꾸면 캘린더 원 색도 바뀐다

## 7. 날짜별 메모
- [ ] 캘린더 위에 메모 입력칸이 있다
- [ ] 저장을 누르면 선택한 날짜에만 저장된다
- [ ] 다른 날짜를 누르면 그 날짜의 메모(또는 빈 칸)가 나온다
- [ ] 저장하지 않고 날짜를 바꾸면 어떻게 되는지 확인 (내용이 사라져도 되는지 결정하기)
- [ ] 메모가 있는 날짜에 작은 점이 표시된다
- [ ] 메모를 전부 지우고 저장하면 점이 사라진다

## 8. 할 일 상세 창
- [ ] 할 일을 누르면 화면 중앙에 높이 약 2/3 크기의 창이 뜬다
- [ ] 배경이 어두워지고, 바깥을 누르면 닫힌다
- [ ] 편집으로 이름을 바꿀 수 있다
- [ ] 삭제하면 "실행 취소" 토스트가 나오고, 누르면 복구된다
- [ ] 할 일 메모가 저장되고, 다시 열면 남아 있다
- [ ] 오늘/미래 할 일: "내일 하기"를 누르면 다음 날로 이동한다
- [ ] 지난 날짜 할 일: 버튼이 "오늘 하기"로 바뀌고, 누르면 오늘로 이동한다
- [ ] 날짜 바꾸기 → 캘린더에서 날짜 선택 → Confirm을 눌러야만 이동한다
- [ ] 날짜만 고르고 Confirm 없이 닫으면 이동하지 않는다
- [ ] 이동 후 원래 날짜와 새 날짜의 캘린더 숫자가 모두 맞게 바뀐다

## 9. 루틴
- [ ] 매일 루틴을 만들면 이후 모든 날짜에 할 일이 나온다
- [ ] 매주(예: 월·수·금) 루틴이 해당 요일에만 나온다
- [ ] 매월(예: 15일) 루틴이 매달 15일에만 나온다
- [ ] 31일로 설정한 매월 루틴이 30일까지인 달에서 어떻게 되는지 확인 (결정하기)
- [ ] 시작일 이전 날짜에는 나오지 않는다
- [ ] 종료일을 정하면 그 이후에는 나오지 않는다
- [ ] 루틴 할 일에 ↻ 아이콘이 있다
- [ ] 루틴 할 일 하나를 체크해도 다른 날짜에는 영향이 없다
- [ ] "이 날짜만 삭제"는 그날만 사라진다
- [ ] "이후 전부 삭제"는 그날부터 이후가 사라지고, 이전 날짜 기록은 남는다
- [ ] 미래 날짜 캘린더 숫자에도 루틴 할 일이 포함된다
- [ ] Local Storage에 루틴 할 일이 날짜마다 쌓이지 않는다 (체크·편집한 날짜만 저장)

## 10. 드래그 앤 드롭
- [ ] 할 일을 드래그해서 다른 카테고리로 옮길 수 있다
- [ ] 옮기면 캘린더 원의 색도 새 카테고리 기준으로 바뀐다 (완료된 할 일인 경우)
- [ ] 같은 카테고리 안에서 순서를 바꿀 수 있다
- [ ] 접혀 있는 카테고리로도 옮길 수 있는지 확인
- [ ] 휴대폰 크기에서: 길게 누르면 드래그 시작, 짧게 스와이프하면 그냥 스크롤
- [ ] 드래그 중 화면이 튀거나 할 일이 사라지지 않는다
- [ ] 새로고침해도 옮긴 결과가 유지된다

## 11. 첫 Vercel 배포
- [ ] GitHub 저장소에 코드가 올라가 있다
- [ ] Vercel에서 배포가 성공(Ready)했다
- [ ] 배포된 주소(https://…vercel.app)에서 앱이 열린다
- [ ] 휴대폰으로 그 주소에 접속해도 잘 보인다
- [ ] 코드를 수정하고 push하면 자동으로 다시 배포된다

## 12. 알림

### 12-1. PWA 설정
- [ ] 개발자 도구 → Application → Manifest에 앱 이름·아이콘이 보인다
- [ ] Service Workers에 서비스 워커가 등록(activated)되어 있다
- [ ] 안드로이드/크롬: "앱 설치" 또는 "홈 화면에 추가"가 가능하다
- [ ] 아이폰 Safari: 공유 → 홈 화면에 추가 후, 아이콘으로 열면 주소창 없이 열린다

### 12-2. 알림 UI
- [ ] Hour는 0~23, Minute는 0·5·10…55만 선택된다
- [ ] Hour와 Minute을 고르고 Done을 눌러야 설정된다
- [ ] Done 없이 닫으면 설정되지 않는다
- [ ] Delete를 누르면 알림이 삭제된다
- [ ] 알림이 있는 할 일에 시간이 표시된다
- [ ] 안내 문구(권한 거부, 아이폰 홈 화면 추가, 몇 분 지연 가능)가 상황에 맞게 나온다

### 12-3. 서버 준비
- [ ] Upstash Redis, QStash 가입 완료
- [ ] VAPID 키 생성 완료
- [ ] Vercel → Settings → Environment Variables에 필요한 값이 모두 들어가 있다
- [ ] 비밀값이 코드나 GitHub에 올라가지 않았다 (`.env` 파일이 `.gitignore`에 있는지)

### 12-4. 구독 + 동기화
- [ ] 처음 알림을 설정할 때 권한 요청 팝업이 뜬다
- [ ] 허용 후 Upstash Redis 콘솔에 `sub:{deviceId}` 데이터가 생긴다
- [ ] 알림을 추가/수정/삭제하면 `alarms:{deviceId}`가 바뀐다
- [ ] 할 일을 완료하거나 날짜를 옮기면 서버 알림 목록도 바뀐다
- [ ] 비행기 모드에서 알림을 바꾼 뒤 다시 연결하면 동기화된다

### 12-5. 발송 + 스케줄
- [ ] QStash 콘솔에 5분 간격 스케줄이 등록되어 있다
- [ ] QStash 로그에서 호출이 성공(200)으로 찍힌다
- [ ] 개발 모드 "지금 알림 보내기" 버튼으로 알림이 온다
- [ ] 주소창에서 `/api/send-due`를 직접 열면 거부된다 (서명 검증)
- [ ] 알림을 누르면 앱이 열리고 해당 날짜로 이동한다

### 12-6. 실제 상황 테스트
- [ ] 5~10분 뒤로 알림을 설정하고 **탭을 닫은 상태**에서 알림이 온다
- [ ] 휴대폰 **화면이 꺼진 상태**에서도 알림이 온다
- [ ] 같은 알림이 두 번 오지 않는다
- [ ] 루틴 알림이 다음 날에도 온다
- [ ] 완료한 할 일은 알림이 오지 않는다
- [ ] 삭제한 알림은 오지 않는다
- [ ] 알림 시각이 한국 시간 기준으로 맞다

## 13. 마무리
- [ ] 내보내기로 JSON 파일이 다운로드된다
- [ ] Local Storage를 지운 뒤 가져오기로 데이터가 전부 복구된다
- [ ] 잘못된 파일을 가져오면 에러 안내가 나오고 기존 데이터가 망가지지 않는다
- [ ] 시스템 다크 모드에서 글자가 잘 보인다
- [ ] 모든 버튼·카드·입력창·모달의 모서리가 둥글다
- [ ] 모든 버튼이 손가락으로 누르기 충분히 크다
- [ ] 실제 휴대폰으로 하루 동안 써보고 불편한 점을 메모했다
- [ ] 최종 배포가 완료되었다
